# BUILDOUT ###################################################################################
[buildout]
allow-hosts =
    *

extends =
    buildout/core/base.cfg
    buildout/core/syschecker.cfg
    buildout/core/directories.cfg
    buildout/core/egginstaller.cfg
    buildout/modules/debugging.cfg
    buildout/modules/documentation.cfg
    buildout/modules/testing.cfg
# STACK #######################################
    buildout/stacks/stack_1.cfg
# FRAMEWORK ###################################
    buildout/frameworks/pyramid_1.5.cfg
###############################################
    app.cfg

parts =
    staging.message
    ${base:parts}
    ${base:parts}
    ${syschecker:parts}
    ${directories:parts}
    ${egginstaller:parts}
    ${debugging:parts}
    ${testing:parts}
    ${documentation:parts}
# STACK #######################################
    ${stack_1:parts}
# FRAMEWORK ###################################
    ${pyramid_1.5:parts}
###############################################


# STAGING PREFERENCES ########################################################################
[actual_environment]
# Localhost
domain             = ${staging:domain} 
host-name          = ${staging:host-name} 
hots-ip            = ${staging:hots-ip} 
# Development Repositories
repo-server        = ${staging:repo-server} 
repo-origin        = ${staging:repo-origin} 
branch             = ${staging:branch} 
# Development Application
webaccess-admin    = ${staging:webaccess-admin} 
webaccess-password = ${staging:webaccess-password} 
# Generated  Parameters
log-level          = ${staging:log-level} 
user               = ${environment:USER}
app-repo           = git@${staging:repo-server}:${staging:repo-origin}/${app:name}.git

# PARTS ######################################################################################
[staging.message]
recipe                  = honeypy.recipe.message
message                 = FINISHED: Installed in a DEVELOPMENT environment!

# CUSTOM #####################################################################################
[sources]
app                     = git ${staging:app-repo} branch=${staging:branch}
bootstrap               = git git@github.com:twbs/bootstrap.git egg=false rev=v3.0.0
